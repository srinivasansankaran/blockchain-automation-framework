apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: carriersr-expressapi
  namespace: carriersr-ent
  annotations:
    fluxcd.io/automated: "false"
spec:
  chart:
    path: examples/supplychain-app/charts/expressapp
    git: "ssh://git@github.com/srinivasansankaran/blockchain-automation-framework.git"
    ref: "develop"
  releaseName: carriersrcorda-enterprise-expressapi
  values:
    nodeName: carriersr-expressapi
    metadata:
      namespace: carriersr-ent
    replicaCount: 1
    expressapp:
      serviceType: ClusterIP
      image: index.docker.io/hyperledgerlabs/supplychain_corda:express_app_0.4.2.0
      pullPolicy: Always
      pullSecrets: regcred
      nodePorts:
        port: 3000
        targetPort: 3000
        name: tcp
      env:
        apiUrl: https://carriersr-springboot.carriersr-ent:20001/api/v1
    proxy:
      provider: ambassador
      name: carriersr
      type: corda
      external_url_suffix: rc.dev2.aws.blockchaincloudpoc-develop.com
      peer_name: carriersr
